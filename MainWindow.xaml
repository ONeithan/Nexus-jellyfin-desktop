<Window x:Class="NexusPobreFlix.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Nexus PobreFlix - Player de TV"
        Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#050508"
        WindowStyle="None"
        WindowState="Maximized"
        ResizeMode="NoResize"
        Icon="nexus desktop icone.ico"
        KeyDown="Window_KeyDown">

    <Grid>
        <!-- Elemento WebView2 que renderiza o Jellyfin -->
        <wv2:WebView2 x:Name="webView" Visibility="Collapsed" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

        <!-- Tela de Configuração Inicial Premium (Exibida caso não haja URL salva) -->
        <Grid x:Name="configGrid" Visibility="Visible">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#09090E" Offset="0" />
                    <GradientStop Color="#050508" Offset="0.5" />
                    <GradientStop Color="#140D26" Offset="1" />
                </LinearGradientBrush>
            </Grid.Background>

            <!-- Container Central Centralizado -->
            <Border Width="480" Height="360" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Background="#11111C" CornerRadius="16" BorderBrush="#3D1D6E" BorderThickness="1.5"
                    Padding="30">
                <Border.Effect>
                    <DropShadowEffect Color="#7B2FBE" BlurRadius="25" ShadowDepth="0" Opacity="0.18" />
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- LOGO NEXUS POBREFLIX -->
                    <Image Grid.Row="0" Source="https://raw.githubusercontent.com/ONeithan/Nexus-PobreFlix/main/img/nexus-pobreflix-logo.png" 
                           Height="42" Stretch="Uniform" Margin="0,10,0,25" HorizontalAlignment="Center"/>

                    <!-- Título -->
                    <TextBlock Grid.Row="1" Text="Configurar Servidor Jellyfin" Foreground="#FFFFFF" 
                               FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,8" />

                    <TextBlock Grid.Row="2" Text="Insira o domínio ou IP do seu servidor para conectar a sua biblioteca no layout de TV." 
                               Foreground="#8E8E9E" FontSize="12" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,20" />

                    <!-- Campo de Entrada -->
                    <Grid Grid.Row="3" Margin="0,0,0,20">
                        <Border Background="#1C1C2D" CornerRadius="8" BorderBrush="#2D2D3F" BorderThickness="1">
                            <TextBox x:Name="txtServerUrl" Background="Transparent" BorderThickness="0" 
                                     Foreground="#FFFFFF" FontSize="14" Padding="12,10" VerticalContentAlignment="Center"
                                     GotFocus="TxtServerUrl_GotFocus" LostFocus="TxtServerUrl_LostFocus"
                                     KeyDown="TxtServerUrl_KeyDown"/>
                        </Border>
                        <!-- Marca d'água -->
                        <TextBlock x:Name="lblPlaceholder" Text="Ex: http://oneithan.online ou http://192.168.1.100:8096" 
                                   Foreground="#5E5E6E" FontSize="13" Margin="14,12,0,0" IsHitTestVisible="False" VerticalAlignment="Top"/>
                    </Grid>

                    <!-- Botão de Conectar -->
                    <Button Grid.Row="4" x:Name="btnConnect" Content="Salvar e Conectar" VerticalAlignment="Top" Height="42"
                            Background="#7B2FBE" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" BorderThickness="0"
                            Click="BtnConnect_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#953CE7"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
